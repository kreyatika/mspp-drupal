<div class="container">
    <div class="row">
        <div class="col-lg-10">
            <div class="mb-5">
                <p>
                    Consultez facilement vos résultats en ligne.
                    Entrez simplement votre numéro d'ordre et la catégorie correspondante.
                </p>
               
            </div>

            <div class="row">
                <div class="col-md-12">
                    <div class="card shadow-sm mb-5">
                        <div class="card-body p-4">
                            <form method="post" action="{{ path('result.exam_result') }}">
                                {% if not result %}
                                <div class="row">
                                    <div class="col-md-6">
                                    <div class="form-group mb-4">
                                        <label for="category" class="form-label fw-bold">{{ 'Catégorie'|t }}</label>
                                        <div class="input-group input-group-lg">
                                            <span class="input-group-text bg-light">
                                                <i class="fas fa-graduation-cap"></i>
                                            </span>
                                            <select class="form-select" id="category" name="category" required>
                                                <option value="">{{ 'Choisir une catégorie'|t }}</option>
                                                <option value="infirmier">{{ 'Sciences Infirmières'|t }}</option>
                                                <option value="sage-femme">{{ 'Sage-Femme'|t }}</option>
                                                <option value="auxiliaire">{{ 'Auxiliaire'|t }}</option>
                                                <option value="biologie-medicale">{{ 'Biologie Medicale'|t }}</option>
                                                <option value="medecine">{{ 'Médecine'|t }}</option>
                                                <option value="odontologie">{{ 'Odontologie'|t }}</option>
                                                <option value="pharmacie">{{ 'Pharmacie'|t }}</option>
                                            </select>
                                        </div>
                                    </div>
                                    </div>
                                    <div class="col-md-6">
                                    <div class="form-group mb-4">
                                        <label for="session" class="form-label fw-bold">{{ 'Session'|t }}</label>
                                        <div class="input-group input-group-lg">
                                            <span class="input-group-text bg-light">
                                                <i class="fas fa-calendar-alt"></i>
                                            </span>
                                            <select class="form-select" id="session" name="session" required>
                                                <option value="">{{ 'Choisir une session'|t }}</option>
                                                <option value="2024-11">{{ 'Novembre 2024'|t }}</option>
                                                <option value="2024-07">{{ 'Juillet 2024'|t }}</option>
                                                <option value="2023-11">{{ 'Novembre 2023'|t }}</option>
                                            </select>
                                        </div>
                                    </div>
                                    </div>

                                    <div class="form-group mb-4">
                                        <label for="numero_ordre" class="form-label fw-bold">{{ 'Numéro d\'ordre'|t }}</label>
                                        <div class="input-group input-group-lg">
                                            <span class="input-group-text bg-light">
                                                <i class="fas fa-id-card"></i>
                                            </span>
                                            <input type="text" 
                                                   class="form-control" 
                                                   id="numero_ordre" 
                                                   name="numero_ordre" 
                                                   placeholder="Ex: OAP001" 
                                                   required>
                                        </div>
                                        <small class="form-text text-muted">{{ 'Le numéro se trouve sur votre carte de candidat'|t }}</small>
                                    </div>

                                    <div class="d-grid gap-2">
                                        <button type="submit" class="btn btn-primary btn-lg">
                                            <i class="fas fa-search me-2"></i>{{ 'Consulter mes résultats'|t }}
                                        </button>
                                    </div>
                                {% endif %}

                                {% if result %}
                                    <div class="mt-4">
                                        <h4 class="mb-4">{{ result.first_name }} {{ result.last_name }} - {{ result.order_number }}</h4>

                                        <div class="table-responsive">
                                            <table class="table table-bordered">
                                                <tbody>
                                                    <tr>
                                                        <th class="bg-light">{{ 'Nom'|t }}</th>
                                                        <td>{{ result.last_name }}</td>
                                                    </tr>
                                                    <tr>
                                                        <th class="bg-light">{{ 'Prénom'|t }}</th>
                                                        <td>{{ result.first_name }}</td>
                                                    </tr>
                                                    <tr>
                                                        <th class="bg-light">{{ 'Date de naissance'|t }}</th>
                                                        <td>{{ result.birth_date|date('d/m/Y') }}</td>
                                                    </tr>
                                                    <tr>
                                                        <th class="bg-light">{{ 'NINU'|t }}</th>
                                                        <td>{{ result.ninu }}</td>
                                                    </tr>
                                                    <tr>
                                                        <th class="bg-light">{{ 'Numéro d\'ordre'|t }}</th>
                                                        <td>{{ result.order_number }}</td>
                                                    </tr>
                                                    <tr>
                                                        <th class="bg-light">{{ 'École'|t }}</th>
                                                        <td>{{ result.school_name }}</td>
                                                    </tr>
                                                    <tr>
                                                        <th class="bg-light">{{ 'Année d\'examen'|t }}</th>
                                                        <td>{{ result.exam_year }}</td>
                                                    </tr>
                                                    <tr>
                                                        <th class="bg-light">{{ 'Statut'|t }}</th>
                                                        <td>
                                                            {% if result.passed %}
                                                                <span class="badge bg-success text-uppercase">{{ 'Admis(e)'|t }}</span>
                                                            {% else %}
                                                                <span class="badge bg-danger text-uppercase">{{ 'Échoué(e)'|t }}</span>
                                                            {% endif %}
                                                        </td>
                                                    </tr>
                                                </tbody>
                                            </table>
                                        </div>
                                    </div>

                                    <div class="d-flex gap-3 mt-4">
                                        <a href="{{ path('result.exam_result') }}" class="btn btn-sm btn-outline-primary">
                                            <i class="fas fa-search me-2"></i>{{ 'Nouvelle recherche'|t }}
                                        </a>
                                        <a href="{{ path('<front>') }}" class="btn btn-sm btn-outline-secondary">
                                            <i class="fas fa-home me-2"></i>{{ 'Retour à l\'accueil'|t }}
                                        </a>
                                    </div>
                                {% endif %}
                            </form>

                            {% if error_message %}
                                <div class="alert alert-danger mt-4" role="alert">
                                    <i class="fas fa-exclamation-circle me-2"></i>{{ error_message }}
                                </div>
                            {% endif %}
                        </div>
                    </div>
                </div>
            </div>

            <div class="row">
                <div class="col-md-6">
                    <div class="card border-0 bg-light rounded-3 mb-4">
                        <div class="card-body p-4">
                            <h5 class="card-title fw-bold mb-3">
                                <i class="fas fa-info-circle text-primary me-2"></i>{{ 'Important'|t }}
                            </h5>
                            <p class="card-text">{{ 'Cette plateforme est réservée aux candidates et candidats de l\'examen d\'État en sciences infirmières de la session de Juillet 2025.'|t }}</p>
                        </div>
                    </div>
                </div>

                <div class="col-md-6">
                    <div class="card border-0 bg-light rounded-3">
                        <div class="card-body p-4">
                            <h5 class="card-title fw-bold mb-3">
                                <i class="fas fa-headset text-primary me-2"></i>{{ 'Besoin d\'aide ?'|t }}
                            </h5>
                            <ul class="list-unstyled mb-0">
                                <li class="mb-2">
                                    <i class="fas fa-envelope text-muted me-2"></i>
                                    dfpss@mspp.gouv.ht
                                </li>
                                <li class="mb-2">
                                    <i class="fas fa-phone text-muted me-2"></i>
                                    (+509) 2234-5678
                                </li>
                                <li>
                                    <i class="fas fa-clock text-muted me-2"></i>
                                    {{ 'Lun-Ven, 8h00-16h00'|t }}
                                </li>
                            </ul>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
