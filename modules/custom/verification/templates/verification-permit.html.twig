<div class="row">
    <div class="col-md-8">
        <div class="card shadow-sm mb-5">
            <div class="card-body p-4">
                <form method="post" action="{{ path('verification.permit') }}">
                    {% if not permit_data %}
                        <div class="form-group mb-4">
                            <label for="nif" class="form-label fw-bold">{{ 'Numéro d\'Identification Fiscale (NIF)'|t }}</label>
                                        <div class="input-group input-group-lg">
                                            <span class="input-group-text bg-light">
                                                <i class="fas fa-id-card"></i>
                                            </span>
                                            <input type="text" 
                                                   class="form-control" 
                                                   id="nif" 
                                                   name="nif" 
                                                   placeholder="Ex:000-000-000-0" 
                                                   pattern="[0-9]{3}-[0-9]{3}-[0-9]{3}-[0-9]{1}"
                                                   maxlength="13"
                                                   required>
                                        </div>
                                        <small class="form-text text-muted">{{ 'Entrez votre NIF sans espaces ni tirets'|t }}</small>
                                    </div>

                                    <div class="d-grid gap-2">
                                        <button type="submit" class="btn btn-primary btn-lg">
                                            <i class="fas fa-search me-2"></i>{{ 'Vérifier le permis'|t }}
                                        </button>
                                    </div>
                                {% endif %}

                                {% if permit_data %}
                                    <div class="mt-4">
                                        <div class="alert alert-success mb-4">
                                            <i class="fas fa-check-circle me-2"></i>{{ 'Le permis est authentique et enregistré dans notre base de données.'|t }}
                                        </div>
                                        <h4 class="mb-4">{{ permit_data.first_name }} {{ permit_data.last_name }}</h4>
                                        <div class="table-responsive">
                                            <table class="table table-bordered">
                                                <tbody>
                                                    <tr>
                                                        <th class="bg-light">{{ 'Nom'|t }}</th>
                                                        <td>{{ permit_data.last_name }}</td>
                                                    </tr>
                                                    <tr>
                                                        <th class="bg-light">{{ 'Prénom'|t }}</th>
                                                        <td>{{ permit_data.first_name }}</td>
                                                    </tr>
                                                    <tr>
                                                        <th class="bg-light">{{ 'Numéro d\'Identification Fiscale (NIF)'|t }}</th>
                            <td>{{ permit_data.nif }}</td>
                        </tr>
                        <tr>
                            <th class="bg-light">{{ 'Numéro du permis'|t }}</th>
                            <td>{{ permit_data.permit_number }}</td>
                        </tr>
                        <tr>
                            <th class="bg-light">{{ 'Numéro d\'hologramme'|t }}</th>
                                                        <td>{{ permit_data.hologram_number }}</td>
                                                    </tr>
                                                    <tr>
                                                        <th class="bg-light">{{ 'Catégorie'|t }}</th>
                                                        <td class="text-capitalize">{{ permit_data.category_name }}</td>
                                                    </tr>
                                                </tbody>
                                            </table>
                                        </div>
                                    </div>
                                    
                                    <div class="d-flex gap-3 mt-4">
                                        <a href="{{ path('verification.permit') }}" class="btn btn-sm btn-outline-primary">
                                            <i class="fas fa-search me-2"></i>{{ 'Nouvelle recherche'|t }}
                                        </a>
                                        <a href="{{ path('<front>') }}" class="btn btn-sm btn-outline-secondary">
                                            <i class="fas fa-home me-2"></i>{{ 'Retour à l\'accueil'|t }}
                            </a>
                        </div>
                    {% endif %}
                </form>

                {% if error_message %}
                    <div class="alert alert-danger mt-4" role="alert">
                        <i class="fas fa-exclamation-circle me-2"></i>
                        {{ error_message }}
                    </div>
                {% endif %}
            </div>
        </div>

            <div class="card shadow-sm mb-4">
                <div class="row g-0">
                    <div class="col-md-6">
                        <img src="/themes/custom/mspp/images/permit.png" class="img-fluid rounded-start" alt="{{ 'Image de permis'|t }}">
                    </div>
                    <div class="col-md-6">
                        <div class="card-body">
                            <h5 class="card-title">{{ 'Information utile'|t }}</h5>
                            <p class="card-text">{{ 'La vérification des permis est un service gratuit offert par le MSPP pour assurer l\'authenticité des permis d\'exploitation.'|t }}</p>
                        </div>
                    </div>
                </div>
            </div>


    </div>
    <div class="col-md-4">
        <div class="card shadow-sm mb-4">
            <div class="card-body">
                <h5 class="card-title">{{ 'Information utile'|t }}</h5>
                <p class="card-text">{{ 'La vérification des permis est un service gratuit offert par le MSPP pour assurer l\'authenticité des permis d\'exploitation.'|t }}</p>
            </div>
        </div>

        <div class="card shadow-sm">
            <div class="card-body">
                <h5 class="card-title">{{ 'Liens rapides'|t }}</h5>
                <ul class="list-unstyled mb-0">
                    <li class="mb-2">
                        <a href="{{ path('<front>') }}" class="text-decoration-none">
                            <i class="fas fa-home me-2"></i>
                            {{ 'Page d\'accueil'|t }}
                                    </a>
                                </li>
                                <li class="mb-2">
                                    <a href="#" class="text-decoration-none">
                                        <i class="fas fa-info-circle me-2"></i>{{ 'À propos des permis'|t }}
                                    </a>
                                </li>
                                <li class="mb-2">
                                    <a href="#" class="text-decoration-none">
                                        <i class="fas fa-question-circle me-2"></i>{{ 'FAQ'|t }}
                                    </a>
                                </li>
                                <li>
                                    <a href="#" class="text-decoration-none">
                                        <i class="fas fa-envelope me-2"></i>{{ 'Contactez-nous'|t }}
                                    </a>
                                </li>
                            </ul>
                        </div>
                    </div>
                </div>
            </div>
