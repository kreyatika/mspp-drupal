{#
/**
 * @file
 * Theme override for Alertes Sanitaires view row fields.
 */
#}
<div class="border-bottom border-secondary-subtle py-3">
  {# Title #}
  <a href="{{ fields.field_publication_file.content|striptags|trim }}" class="text-dark text-decoration-none" target="_blank">
    <h6 class="mb-2 fw-normal">{{ fields.title.content }}</h6>
  </a>
  
  {# Date #}
  <p class="text-muted mb-0 small">
    {{ fields.created.content }}
  </p>
</div>
<style>
/* Container spacing refinement */
.alertes-sanitaires .border-bottom {
    border-color: #e8e5e5 !important;
    padding-top: 0.85rem !important;
    padding-bottom: 0.85rem !important;
}

/* Title styling */
.alertes-sanitaires h6 {
    font-size: 0.98rem; /* slightly smaller for compactness */
    line-height: 1.35;
    margin-bottom: 0.35rem !important; /* tighter link → date */
    font-weight: 500;
    color: #1a1a1a;
}

/* Link behavior */
.alertes-sanitaires a {
    color: inherit;               /* keeps text-dark behavior */
    text-decoration: none; 
}

.alertes-sanitaires a:hover h6 {
    text-decoration: underline;   /* professional interactive state */
    color: #C40000;               /* same red as alert icon */
}

/* Date styling */
.alertes-sanitaires p.small {
    margin-top: 0 !important;
    font-size: 0.82rem;
    color: #6e6e6e !important;    /* softer secondary grey */
}
</style>