{# siteheader #}
{{ page.header }}
{# siteheader #}

{# menu #}
{{ page.menu }}
{# menu #}

{{ drupal_block('hero_section', wrapper=false) }}

    <div
    class="container"> {# Header + CTA on the same row #}
    <div class="row align-items-center mb-4">
        <div class="col-md-6">
            <h3 class="mb-0">Dernières actualités</h3>
        </div>
    </div>

    {# Content columns #}
    <div class="row g-4">
        <div class="col-lg-8">
            {{ drupal_view('latest_news') }}
        </div>

        <div class="col-lg-4">
            {{ drupal_view('alertes_sanitaires') }}
        </div>
    </div>
</div>

<div class="container py-4">
    <hr>
</div>

<div class="container">
    <div class="row">
        <div class="col-sm-12">
            <h3 class="mt-2 mb-4">
                Le Ministre
            </h3>
        </div>
    </div>
</div>

<div class="container">
    {{ drupal_block('the_minister') }}
</div>

<div class="container py-4">
    {# Section title #}
    <div class="row mb-4">
        <div class="col-lg-12">
            <h3 class="mb-0">Nos services et outils</h3>
        </div>
    </div>
</div>

<div class="bg-primary">
    <div class="container py-5">
        <div class="row g-4">
            {# Left content: services list + button #}
            <div class="col-lg-8">
                {{ drupal_view('services_featured_list') }}
            </div>

            {# Right content: featured block #}
            <div class="col-lg-4">
                {{ drupal_block('service_featured') }}
            </div>

        </div>
    </div>
</div>

<div class="container mt-4">
    <a href="" class="btn btn-outline-primary">
        Tous nos services
        <i class="fa fa-arrow-right"></i>
    </a>
</div>

<div class="container py-3">
    <hr>
</div>

<div class="container">
    {{ drupal_view('video_news') }}
</div>

{{ page.footer }}